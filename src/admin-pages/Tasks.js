import React from 'react';

const Tasks = () => {
  return (
    <div style={pageStyles.pageContainer}>
      <h2>Tasks</h2>
      <p>Tasks</p>
    </div>
  );
};

const pageStyles = {
  pageContainer: {
    padding: '20px',
  },
};

export default Tasks